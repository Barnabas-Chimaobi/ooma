import {StyleSheet} from 'react-native';

export const StyleFoot = StyleSheet.create({
  footer: {
    position: 'absolute',
    bottom: 0,
    width: '100%',
    // backgroundColor: '#ffffff',
  },
});
